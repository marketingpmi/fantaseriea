import { ConvertPhotoUrlPipe } from './convert-photo-url.pipe';

describe('ConvertPhotoUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertPhotoUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
