import { GetPhotoUrlFromNamePipe } from './get-photo-url-from-name.pipe';

describe('GetPhotoUrlFromNamePipe', () => {
  it('create an instance', () => {
    const pipe = new GetPhotoUrlFromNamePipe();
    expect(pipe).toBeTruthy();
  });
});
